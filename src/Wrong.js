import React from "react";

const Wrong = () => {
  return (
    <div>
      <button onClick={() => alert(`Answer is "Digital Evolution"`)}>
        click
        {/* sjvn */}
      </button>
    </div>
  );
};

export default Wrong;
